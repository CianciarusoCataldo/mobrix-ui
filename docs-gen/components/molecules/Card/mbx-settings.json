{
  "since": "1.0.0",
  "description": "A Card component. Its UI depends on given parameters (header, body and footer)",
  "see": [],
  "examples": [
    "import { render } from 'react-dom'; import { Card } from 'mobrix-ui'; render(<Card header='Example header' body='Example body' footer='Example footer' />, document.getElementById('root'));"
  ],
  "extend": {
    "group": {
      "ComponentWithChildren": {
        "type": "`JSX.Element` | `string`"
      }
    }
  }
}
