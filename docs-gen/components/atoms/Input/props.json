{
  "value": { "type": "string", "description": "text input value" },
  "placeholder": {
    "type": "string",
    "description": "label showed when no value is set"
  },
  "readOnly": {
    "type": "boolean",
    "description": "if true, component value can only be set with `value` parameter",
    "default": "false"
  },
  "onChange": {
    "type": "(newValue: string) => void",
    "default": "() => {}",
    "description": "Callback triggered when Input component input value is changed by the user"
  }
}
