{
  "value": {
    "type": "boolean",
    "description": "Checkbox initial value (checked / unchecked)",
    "default": "false"
  },
  "onChange": {
    "type": "(newValue: boolean) => void",
    "description": "callback called when Checkbox is clicked (when its state changes)"
  },
  "icon": {
    "type": "JSX.Element",
    "description": "custom tick icon (if not set, the default one will be used)",
    "default": "Internal tick icon"
  }
}
