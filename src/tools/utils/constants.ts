import { CommonProps } from "../../types";

export const DEFAULT_COMMON_PROPS: CommonProps = {
    noBackground: false,
    noHover: false,
    a11y: true,
    disabled: false,
    additionalProps: {}
}